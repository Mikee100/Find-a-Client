# Frontend

Place your frontend app here.

Suggested options:
- Next.js (recommended for SEO and content-heavy discovery pages)
- React + Vite (recommended for fast SPA setup)

Expected scripts for CI:
- `npm run lint`
- `npm test`
- `npm run build`
