"use client";

import AdminShell from "@/features/admins/admin-shell";

export default function AdminConfigurationsPage() {
  return (
    <AdminShell title="Configurations" subtitle="Control feature flags and platform-level configuration.">
      <section className="rounded-xl border border-slate-200 bg-white p-4 text-sm text-slate-600 shadow-sm">
        Configuration controls are coming next.
      </section>
    </AdminShell>
  );
}
