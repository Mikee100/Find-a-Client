# Backend

Place your backend app here.

Suggested options:
- Node.js + NestJS (structured, scalable)
- Node.js + Express (lightweight)

Expected scripts for CI:
- `npm run lint`
- `npm test`
- `npm run build`
